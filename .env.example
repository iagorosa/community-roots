# Consumed by docker-compose.yml.
# Copy to .env and adjust: cp .env.example .env

POSTGRES_USER=community_roots
POSTGRES_PASSWORD=change-me-locally
POSTGRES_DB=community_roots

# Host port for the database container.
# 5433 and 54322 are already taken by other Postgres containers on this machine.
POSTGRES_PORT=5432
