/**
 * Placeholder for issue #14 (routing only). The real interactive map —
 * `PlantingMap`, `fitBounds`, loading/error states — is issue #18.
 */
function MapPage() {
  return (
    <div className="flex flex-1 items-center justify-center bg-slate-50">
      <h1 className="text-2xl font-bold text-slate-700">Mapa</h1>
    </div>
  )
}

export default MapPage
